`★ Insight ─────────────────────────────────────`
The design workflow enforces a **preview-then-commit** pattern: preview SVGs go under `areas/` with option suffixes, and only after explicit approval do we write the canonical `-layout.svg` and update all related files (area `.md`, `calendar.md`, `log/`) in a single turn. This prevents partial saves and keeps the garden state consistent.
`─────────────────────────────────────────────────`

All files updated:

- **Layout SVG** — fire pit moved to east, 2 raised beds on west
- **Area file** — updated features, zone summary, and key decisions
- **Log** — appended revision entry for 2026-02-21
- **Calendar** — added action items for the physical move and bed removal in March

Saved: areas/backyard-south-layout.svg, areas/backyard-south.md, log/2026-02.md, calendar.md
